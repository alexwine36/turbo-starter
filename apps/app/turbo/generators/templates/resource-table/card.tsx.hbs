'use client'

import {
Card,
CardContent,
CardHeader,
CardTitle,
} from '@repo/design-system/components/ui/card';
import { {{ capitalizedName }}Dialog } from '../{{ pathName }}-dialog';
import { {{ capitalizedName }}Table } from '../{{ pathName }}-table';
import { {{ capitalizedName }}Types } from '../{{ pathName }}-types';
import React from 'react';


export const {{ capitalizedName }}Card: React.FC<{{ capitalizedName }}Types> = (props) => {
  const [open, setOpen] = React.useState(false);

  return (
  <Card>
    <CardHeader>
      <CardTitle>
        <div className="flex items-center gap-2">
          {{ capitalizedName }}
          {{#if includeMethods.create}}
          <{{ capitalizedName }}Dialog open={open} onOpenChange={setOpen} showTrigger {...props} />
          {{/if}}
        </div>
      </CardTitle>
    </CardHeader>
    <CardContent>
      <{{ capitalizedName }}Table {...props} />
    </CardContent>
  </Card>
  )
  }