'use client'

import {
  Card,
  CardContent,
  CardHeader,
  CardTitle,
} from '@repo/design-system/components/ui/card';
import { {{ capitalizedName }}Dialog } from '../{{ propertyName }}-dialog';
import { {{ capitalizedName }}Table } from '../{{ propertyName }}-table';
import React from 'react';


export const {{ capitalizedName }}Card: React.FC = () => {
    const [open, setOpen] = React.useState(false);

    return (
        <Card>
          <CardHeader>
            <CardTitle>
              <div className="flex items-center gap-2">
                {{ capitalizedName }}
                <{{ capitalizedName }}Dialog open={open} onOpenChange={setOpen} showTrigger />
              </div>
            </CardTitle>
          </CardHeader>
          <CardContent>
            <{{ capitalizedName }}Table />
          </CardContent>
        </Card>
    )
}