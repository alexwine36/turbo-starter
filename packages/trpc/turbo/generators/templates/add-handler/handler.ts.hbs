import type { TRPCContextInner } from '@/server/create-context';
import type { {{ schemaName }} } from './{{ baseHandlerFile }}-schema.ts'

type {{ handlerOptions }} = {
    ctx: TRPCContextInner;
    input: {{ schemaName }};
}

export const {{ handlerName }} = async ({ ctx, input }: {{handlerOptions}}) => {

    const { prisma, session } = ctx;
    
    const res = []

    return res;
}

export type {{ handlerResponse }} = Awaited<ReturnType<typeof {{ handlerName }}>>;